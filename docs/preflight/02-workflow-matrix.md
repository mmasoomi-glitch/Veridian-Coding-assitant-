# 02 — Workflow Matrix (Veridian)

> End-to-end trace per feature: UI → validation → API → authz → backend → persistence → output → survives-refresh → error/recovery. A workflow is **PASS** only with end-to-end proof AND no blocking dependency. Status legend: PASS (proven, safe) · PARTIAL (works but degraded/unproven leg) · FAIL (broken, unsafe, or core dependency unmet).

| Workflow | Intended Outcome | UI Evidence | API Evidence | Database Evidence | Integration Evidence | Failure Recovery | Status |
|---|---|---|---|---|---|---|---|
| Telemetry capture (Windows) | Live machine state shown | Dashboard HUD (`App.tsx`) — but seeded with fake mira-vpn until first poll (`App.tsx:70-89`) | `/api/telemetry/current` returns real data (live verified) | `workspace-sessions.json` rolling, cap 60 (`persist.ts`) | `collect.ps1` Win32 — Windows only | Poller silent-catches write errors; no recovery UI | PARTIAL (works on Windows; fake initial state; blind on cloud) |
| Telemetry → AI brief ("Where was I?") | Opus brief from real state | "Ask AI Where was I?" button (`App.tsx:892`); fallback heuristic message (`App.tsx:303-323`) | `/api/ai/summarize` → 503 if no provider, else DeepSeek | brief not separately persisted | **DeepSeek active, NOT Claude Opus** (`providers.ts:24`; env key present) | Heuristic fallback when provider null; generic error "Verify your API configuration" | FAIL (wrong brain per PDR; integration mocked vs intent) |
| AI Ask (context Q&A) | Grounded answer over local context | `AiAskTab.tsx` input (no `<label>`) | `/api/ask` works; rejects empty Q | `ask-history.json` — **contains leaked `sk-ant-*` keys** (verified grep) | Sends full clipboard/sessions/briefs to DeepSeek | error box shown on failure | FAIL (secret leakage + sends context to 3rd-party LLM, no auth) |
| Autopilot next-step | Propose safe next action | CommandDeck "What now?" | `/api/autopilot/next` returns confidence/safety | `autopilot-learning.json` trust counts (verified ok) | depends on LLM provider | gated for irreversible actions | PARTIAL (logic OK; rides broken provider) |
| Autopilot fleet (assess/build/full) | Headless Opus advances project | `AutopilotFleet.tsx` mode dropdown (global, not per-project) | `/api/fleet/run`, `/api/fleet/status` | `fleet-progress.json` real assess runs (06-22..06-24) | spawns `claude -p --model opus`; **no scheduler** (overnight mode TODO) | stale "running" not cleared on crash | PARTIAL (one-shot assess proven; build/full + overnight unproven/unsafe) |
| Per-desktop sessions/briefs | Resume context per desktop | desktop switch UI in CommandDeck | `/api/desktop/briefs`, `/api/desktop/info` (verified) | `desktop-briefs.json` (also holds residual fake data) | `desktop-switch.ps1` Windows only, untested edge cases | brief recall works | PARTIAL (briefs persist; switch untested; persistent interactive sessions still TODO) |
| Clipboard (20 + autocomplete + 5 pinned) | History + restore | `ClipboardTab.tsx` autocomplete; dashboard widget read-only | `/api/clipboard/history` (20), `/top` (5), `/suggest` (verified) | `clip-history.json` + `clip-counts.json` — **raw secret `value` persisted unredacted** | Win `Set-Clipboard` restore | preview redacted for UI only | PARTIAL (display works; raw secret-at-rest leak; Linux restore missing) |
| Screenshots (auto >1min) | Per-desktop captures | `ScreenshotsTab.tsx` gallery + lightbox | `/api/screenshots` (200 indexed), `/img/:id` (no auth) | `screenshots-index.json` cap 200 + PNGs | `screenshot.ps1` Windows only | orphan PNGs possible on crash; no cleanup | PARTIAL (works on Windows; unauth'd remote access; no user controls) |
| Todo | Create/toggle/persist | `TodoTab.tsx` (no label) | `/api/todos` GET/POST/toggle; **accepts empty text**; bad id → 200 null | `todos.json` persists across refresh (verified) | none | no confirm on delete | PARTIAL (CRUD works; weak validation; no delete confirm) |
| Scratch buffer | Typing-recovery buffer | scratch UI | `/api/scratch` **accepts empty/whitespace** | scratch snapshots | none | concurrent writes lose data (race) | PARTIAL |
| Keystroke recorder | Record on demand | `KeyloggerTab.tsx` start/pause/clear | `/api/keylog*` (no auth) live `recording:false` | `keystroke-log.txt` | `keylog.ps1` Windows only; **not auto-started** | dupe processes on restart (PID race) | PARTIAL (manual only; inert by default; unauth'd plaintext exposure) |
| Burnout detection | Nudge when fatigued | `BurnoutNudge.tsx` polls 30s | `/api/burnout` live `level:"ok", reasons:["no data"]` | none | depends on keystroke metrics that don't run | nudge never fires | FAIL (inert — never leaves "ok"; core promise unmet) |
| Backup → Hetzner | scp to volume | none surfaced | `/api/backup`, `/api/backups` returns empty local+remote | backup log | scp/ssh to `89.167.49.209`; key unverified | silent failure, returns ok:true; no integrity check | FAIL (unproven; silent-fail; restore unverified) |
| Sync / central command | Aggregate machines | `MachineSelector.tsx` stub | `/api/sync/push`, `/api/sync/machines` (stale entry) | `sync-machines.json` | `CENTRAL_URL` **unset** → disabled | silent network swallow | FAIL (disabled; no pull/UI; would leak clipboard if enabled) |
| TOTP auth (cloud) | Gate cloud access | `LoginGate.tsx` QR/recovery | `/api/auth/*`; gate only if `VERIDIAN_AUTH=totp` | TOTP secret file | HMAC session, timing-safe (good) | no 401 re-auth handling in UI | PARTIAL (crypto sound; OFF by default; setup IP-check weak behind proxy) |
| Deploy / cloud | Run on Hetzner | n/a | server boots on Linux | n/a | **PowerShell features cannot run on Linux**; no systemd/nginx/deploy docs | none | FAIL (Windows features blind; no reproducible deploy) |
| APK | Companion viewer | Capacitor build | reads PC API | n/a | build undocumented, not reproducible; no CI | offline = stale/blank | FAIL (unproven, undocumented build) |
| Electron .exe | Portable desktop app | `electron/main.cjs` spawns server | bundled | data under AppData `resourcesPath` | no first-run config for repo/keys; no `.env` shipped | none | FAIL (no config path; AI unconfigured on clean install) |
| Voice narration | Natural voice | `App.tsx:334-426` narrate button | `/api/elevenlabs/tts` proxy | localStorage key | ElevenLabs BYOK else Web Speech fallback | graceful fallback | PASS (works; "natural" only with BYOK key — intent caveat) |
| Tabbed UI shell | 7 tabs navigation | `TabbedApp.tsx` 7 tabs animated | n/a | n/a | motion/react | n/a | PASS |
| Clipboard secret redaction (display) | Hide secrets in UI | Lock icon | `/api/sessions` shows `ANTH…[redacted]` | redacted preview in list endpoint | regex `redactSecret` (`server.ts:182-192`) | n/a | PASS for display only (raw still persisted at rest — see findings) |
