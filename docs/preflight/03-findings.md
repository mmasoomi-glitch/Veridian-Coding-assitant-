# 03 — Findings (deduped, lead-auditor synthesis)

> All 10 specialists' findings deduped and re-severitized by the lead auditor. Hard evidence (`file:line`, command output) preserved. Severity: BLOCKER (ship-stopper, unsafe/deceptive/insecure core) · CRITICAL (must fix before human testing) · MAJOR · MINOR. Release Gate per finding: REJECT / CONDITIONAL / OK. Several specialist "REJECT" gates on MAJOR/MINOR items were normalized down where the finding does not by itself block.

| ID | Severity | Area | User Impact | Evidence | Reproduction Steps | Root Cause | Recommended Fix | Release Gate |
|---|---|---|---|---|---|---|---|---|
| F-001 | BLOCKER | AI provider (C1/C2) | Wrong AI brain: DeepSeek serves all AI, not the PDR-mandated Claude Opus. Contradicts core "powerful AI" intent and cost model. | `printenv` → `DEEPSEEK_API_KEY=sk-577ea1a0...` present, `AI_PROVIDER` unset; `ai/providers.ts:16-26` returns `deepseek` (line 24); `.env` comment literally says "DeepSeek is the fallback / current working provider"; UI label "DeepSeek Smart Recall" `App.tsx:768`; App comments "Ask DeepSeek" `App.tsx:283,328` | 1. `npm run dev` 2. POST `/api/ai/summarize` 3. Response from DeepSeek not Claude 4. `printenv \| grep DEEPSEEK` returns key | DeepSeek key left in env; no enforcement of PDR "no DeepSeek"; `AI_PROVIDER=claude` never set | Set `AI_PROVIDER=claude` + verify `claude` CLI logged in; remove `DEEPSEEK_API_KEY` from env; add startup assertion; fix UI/comment labels | REJECT |
| F-002 | BLOCKER | Auth — all data endpoints ungated by default | Anyone reaching the port reads ALL clipboard (incl. secrets), keystrokes, screenshots, telemetry, briefs, todos, AI history — no credentials. | `auth/totp.ts:71-72` gates only if `VERIDIAN_AUTH==='totp'` (default OFF); live `/api/auth/status`→`{"required":false}`; `server.ts:64-71` middleware short-circuits when not required; `curl /api/clipboard/history`, `/api/keylog`, `/api/screenshots`, `/api/sessions` all return data unauthenticated | 1. Leave `VERIDIAN_AUTH` unset 2. `curl http://<host>/api/clipboard/history` → full history incl. secrets | Auth is opt-in runtime config, not a built-in boundary; no per-endpoint authz | Require TOTP by default on non-localhost; gate every data endpoint; fail-closed | REJECT |
| F-003 | BLOCKER | Secrets persisted in plaintext + leaked back via AI Ask | Real `sk-ant-*` Anthropic key, base URL, Hetzner creds stored unredacted on disk AND echoed by `/api/ask`. | `clip-history.ts:148` persists raw `value`; `clip-history.ts:113-114` `clip-counts.json` also stores raw `value`; live grep: `sk-ant` present in `clip-history.json`, `clip-counts.json`, AND `ask-history.json`; `redactSecret` only redacts the UI *preview*, not the stored value | 1. Copy an API key 2. `cat clip-history.json` → full key in `value` 3. POST `/api/ask` "what API keys have I used?" → key echoed | Frequency/restore design stores raw value; ask-history trusts prior LLM output containing keys; no encryption at rest | Never persist raw secret values; store redacted preview + hash only; scrub `ask-history.json`; **rotate the exposed `sk-ant-...` key now**; regex-scrub before LLM send | REJECT |
| F-004 | BLOCKER | Cloud cannot run Windows-only features | Cloud "central command" (`pr.afaq24.store`, Linux) cannot collect telemetry/screenshots/keystrokes/desktop — all are `.ps1`. Cloud shows empty/garbage. | `server.ts` spawns `powershell.exe` (`server.ts:162-176`); `collect.ps1`/`screenshot.ps1`/`keystroke-metrics.ps1`/`keylog.ps1`/`desktop-switch.ps1` all Win32/.NET | Deploy to Ubuntu; call `/api/telemetry/current` → powershell not found | Architecture assumes single Windows machine; aggregator is itself Linux; no platform abstraction | Split collection (Windows agent) from aggregation (Linux read-only); wire `sync-client` push; document cloud is read-only | REJECT |
| F-005 | BLOCKER | No automated tests / no acceptance plan (C7) | Zero regression protection; release readiness undefined for cloud, APK, .exe. | No `*.test.*`/`*.spec.*` (live find: none); `package.json` scripts = dev/build/start/lint/electron/dist:win only — no `test`; no CI; intent C7 confirmed | `npm test` → no script; find tests → none | Testing never bootstrapped; feature-first from simulator | Add vitest; integration tests for telemetry/AI/clipboard-redaction/fleet/auth; smoke test; this audit's doc 06 is the interim acceptance plan | REJECT |
| F-006 | BLOCKER | Seeded fake "Mira VPN" state on first load (C6) | First render shows fake workspace (mira-vpn, auth.service.ts, fake secret) before real telemetry — deceptive; owner explicitly hates fake data. | `App.tsx:70-89` hardcodes `virtualDesktop:"Desktop 2 (Mira VPN Dev)"`, `gitRepo:"mira-vpn"`, `clipboardContent:"eyKey:'v_prod_9921_xzz_k9'"`; same in `loadScenario()` ~`App.tsx:432-451`; residual fake data in `desktop-briefs.json` + `screenshots-index.json`; demo buttons `SensorsSimulator.tsx:46-513` | 1. `npm run dev` 2. open localhost:3000 3. HUD shows "MIRA-VPN" before poll | Demo seed never removed; comment "(Simulated)" baked into initial `useState` | Initialize state empty; remove/flag demo scenarios behind dev mode; wipe fake JSON seed data | REJECT |
| F-007 | BLOCKER | Background capture not auto-started (burnout + keystrokes inert) | "Proactive" burnout/keystroke features never run unless user manually starts recorder AND sets `TELEMETRY_POLL_MS`. Burnout always "ok". | `server.ts:295-308` poller no-ops if `TELEMETRY_POLL_MS` unset/<5000; `keylog-store.ts` `startRecorder()` only via POST `/api/keylog/start`; no auto-start in `startServer()`; live `/api/burnout`→`level:"ok","reasons":["no data"]`; live `/api/keylog`→`recording:false` | 1. Fresh start 2. `/api/burnout` → "no data" forever 3. nudge never appears | Features wired as opt-in sensors, never auto-initialized; contradicts proactive intent | Decide opt-in vs auto; if proactive, auto-arm with consent banner + document `TELEMETRY_POLL_MS` as required | REJECT |
| F-008 | CRITICAL | IP/proxy bypass on `/api/auth/setup` | Behind nginx proxy without `trust proxy`, setup (TOTP secret + recovery codes) may be reachable remotely; attacker can self-enroll. | `server.ts:85-89` allows when `req.hostname` localhost OR `req.ip` includes 127.0.0.1; no `app.set('trust proxy')`; on a proxy `req.ip` = proxy loopback → check falsely passes | `curl http://127.0.0.1:3000/api/auth/setup` returns QR+secret+recovery (confirmed) | IP/hostname checks insufficient for authn; proxy not trusted-configured | Lock setup to post-login or a first-boot provisioning token; set `trust proxy`; never serve recovery codes via API | REJECT |
| F-009 | CRITICAL | Clipboard/keystroke would sync to shared commerce VPS | If `CENTRAL_URL` set, raw `currentState` (incl. clipboard) pushed to shared Hetzner box running other tenants' commerce; unencrypted at rest. | `sync-client.ts:37-60` posts machine snapshot; `sync-store.ts:52-79` stores raw `currentState` in `sync-machines.json`; intent C8 "shared with other people"; currently `CENTRAL_URL` unset (mitigated now) | Set `CENTRAL_URL=https://pr.afaq24.store`, copy a secret, observe push payload | Sync aggregates raw state with no field filtering/encryption on shared infra | Never sync clipboard/keystroke; sync only project ids/progress; encrypt; dedicated VPS | REJECT |
| F-010 | CRITICAL | Keystroke log fully exposed via unauth'd `/api/keylog` | If recording on, full plaintext keystrokes (passwords, code) readable remotely. | `keylog-store.ts:27-36` `recentLog()` returns tail unfiltered; `server.ts:600` GET `/api/keylog` no auth | POST `/api/keylog/start`; type; `curl /api/keylog` → text field | "local only" by comment but exposed over HTTP with auth off | Require auth/local-only on keylog endpoints; never serialize keystrokes to network | REJECT |
| F-011 | CRITICAL | Screenshots unauth'd + predictable IDs | All auto-screenshots (may contain secrets) downloadable via `/api/screenshots/img/:id` with no auth; IDs are `Date.now()` (enumerable). | `screenshots-store.ts:62` `id:String(Date.now())`; `server.ts:584-588` no auth; live `/api/screenshots` returns 200 indexed | `curl /api/screenshots` → paths; `curl /api/screenshots/img/<ts>` → PNG | Treated as local telemetry but HTTP-exposed; predictable IDs | Auth + random UUIDs + at-rest encryption; not captured on cloud | REJECT |
| F-012 | CRITICAL | AI Ask context leakage to 3rd-party LLM, unauth'd | `/api/ask` gathers clipboard+sessions+briefs+notes and ships to DeepSeek/OpenAI; endpoint unauthenticated; provider may log. | `ai-ask.ts:149-179` gathers context; `server.ts:577-580` no auth; `providers.ts:86-125` sends to API | `curl -X POST /api/ask -d '{"question":"latest clipboard"}'` returns `usedContext` + content | No auth + 3rd-party transmission of private context | Auth on `/api/ask`; prefer local Claude; disclose provider logging | REJECT |
| F-013 | CRITICAL | File persistence race conditions (no locking) | Concurrent writes corrupt/lose data across nearly all stores (todos, clipboard, scratch, notebooks, sessions). | `server.ts:111`, `persist.ts:51`, `todo-store.ts:47`, `desktop-briefs.ts:28`, `clip-history.ts`, `notebook.ts:44` all bare `writeFileSync`, no lock; only `sessions.ts:61-73` does temp+rename; live: 5 concurrent `/api/scratch` → only 1-2 snapshots survive | Fire 5-10 concurrent POSTs to a store; observe lost writes | No file locking/mutex/transaction; single-process assumption | Add `proper-lockfile` or migrate to SQLite; central write queue | REJECT |
| F-014 | CRITICAL | Data loss on crash / corrupt-read silently truncates | Crash mid-write or a truncated file → parse fails, returns `[]`, next write overwrites entire file (all prior data lost). | `todo-store.ts:23-26`, `persist.ts:40-47` catch parse errors → return empty; no WAL/journal/backup | Truncate `todos.json`; start server; POST a todo → file now has only the new item | No durability, no atomic rename guarantee on NTFS, no checksum | SQLite or temp+fsync+rename + rolling backups + schema validation | REJECT |
| F-015 | CRITICAL | ID collisions (`Date.now()` only) | Items created in same ms collide → silent overwrite / broken restore. | `todo-store.ts:66`, `screenshots-store.ts:62` pure `String(Date.now())`; `clip-history.ts:143` weak jitter; `pdr.ts:120` | Create 100 todos in tight loop; inspect duplicate ids | ms-precision time as id; no UUID | Use `crypto.randomUUID()`/nanoid; migrate existing | REJECT |
| F-016 | CRITICAL | No-confirmation destructive actions | One click clears all telemetry/clipboard/todos/projects; no confirm, no undo. | `App.tsx:923-925` `clearTimeline()` no `confirm()`; `ClipboardTab.tsx:225`, `TodoTab.tsx:113-125`, `AutopilotFleet.tsx:279`, `Copybook.tsx:258` | Click "Clear Logs" → 60+ events deleted instantly | Destructive handlers mutate without guard | Confirmation dialog on all destructive actions | REJECT |
| F-017 | CRITICAL | No 401/session-expiry handling in UI | After cloud session expires, mutations 401 silently; user believes saved; changes lost. | `TodoTab.tsx:102-111`, `ClipboardTab.tsx:86-100` don't check 401; no global interceptor | Expire session; toggle a todo → "failed" with no re-auth prompt | No unified auth-error handling | Global fetch wrapper → redirect to LoginGate on 401 | CONDITIONAL |
| F-018 | CRITICAL | Refresh/restart loses in-flight state; no recovery | Refresh loses drafts/tab/scroll; server restart orphans fleet runs ("running" stale); no recovery UI. | React state not persisted except elevenLabs key (`App.tsx:96`); no stale-run cleanup; `CommandDeck`/`AutopilotFleet` bare fetch | Draft a fleet project; Ctrl+R → gone; crash mid-run → status stuck running | Ephemeral state; in-memory timers; no restore | Persist UI state to localStorage; clear stale runs on boot; recovery banner | CONDITIONAL |
| F-019 | CRITICAL | No reproducible cloud deploy (systemd/nginx/certbot absent) | Cannot install/run cloud from docs; no service file, vhost, TLS, deploy script. | No Dockerfile/compose/.service/nginx/deploy docs in repo; `backup.ts` hardcodes host but no provisioning | Fresh VPS: clone, build, `node dist/server.cjs` → localhost only, no proxy/cert | Manual one-off deploy; no IaC | Add `docs/deploy/HETZNER.md`, systemd unit, nginx vhost, certbot, deploy.sh | REJECT |
| F-020 | CRITICAL | Claude CLI dependency undocumented + README contradicts intent | README says "Set DEEPSEEK_API_KEY"; CLAUDE.md says `AI_PROVIDER=claude`; no setup for `claude` CLI; clean install has no working AI. | `README.md:18-20` DeepSeek; `CLAUDE.md:28-33` claude; `fleet.ts:86-89`/`providers.ts:43-62` spawn `claude` with no PATH/login check | Fresh clone, `AI_PROVIDER=claude`, run fleet → "claude: command not found" | README stale after provider strategy change | Rewrite README provider hierarchy; add `CLAUDE_BIN`/`CLAUDE_MODEL` to `.env.example`; startup check + graceful fallback | REJECT |
| F-021 | CRITICAL | Electron .exe has no first-run config | Clean install: no way to set repo dir or API keys; AI returns 503; data lands in AppData resources. | `electron/main.cjs:82-91` defaults `VERIDIAN_WATCH_DIR` to userData; `.env` not shipped (git-ignored); `electron-builder.yml` ships code but no `.env` | Build `dist:win`, run on clean PC → no AI, wrong watch dir | .exe treated as client but is a server needing config | First-run wizard/Settings to set repo+provider+keys; store in native config; ship `.env.template` | REJECT |
| F-022 | MAJOR | Backup/restore unverified + silent fail + no integrity check | Backups silently fail; restore returns ok:true without verifying file count/contents. | `backup.ts` hardcodes `~/.ssh/veridian_deploy`, `root@89.167.49.209`; `/api/backups` returns empty; `backup.ts:224` returns ok without manifest check | POST `/api/backup`; `/api/backups` still empty | SSH key/infra unverified; no checksum/manifest | Verify SSH at startup; manifest+checksum on restore; surface status in UI | CONDITIONAL |
| F-023 | MAJOR | Fleet overnight scheduling missing | Core "wake up to progress" use case impossible; fleet is manual fire-and-forget. | `fleet.ts:120-142` fire-and-forget; no cron; HANDOFF "overnight mode" TODO | Configure project; idle 8h → no new runs | Trigger mechanism never built | Add scheduler + `/api/fleet/schedule`; arm on boot (gated by opt-in mode) | CONDITIONAL |
| F-024 | MAJOR | Fleet permission mode global, not per-project | User can accidentally select FULL (unsupervised) for all; no per-project default. | `AutopilotFleet.tsx:57` global `mode`; HANDOFF TODO per-project modes | Select FULL by accident, Run → unsupervised changes | UI simpler than backend; modes not stored per project | Per-project mode in `fleet-projects.json` + UI selector; default to assess | CONDITIONAL |
| F-025 | MAJOR | Network/offline resilience absent | API failures silent; no offline indicator; no retry/backoff; stale UI on reconnect. | `CommandDeck.tsx:60-72`, `sync-client.ts:57-59` swallow errors; `App.tsx:128-132` 5s poll no error UI | Kill server; UI shows no change/indicator | Optimistic happy-path design | Global heartbeat/offline badge; backoff; cached state overlay | CONDITIONAL |
| F-026 | MAJOR | Input validation gaps (empty todo/scratch, unbounded PDR, bad id → 200) | Empty/whitespace entries pollute stores; oversized PDR can hang LLM; invalid toggle id returns 200 null. | Live: POST `/api/todos {}` → empty todo persisted; `server.ts:592` `String(req.body?.text \|\| '')`; `/api/scratch` `server.ts:642`; PDR no max len; bad id toggle → 200 null `server.ts:593` | POST empty bodies to each endpoint | No server-side validation | Reject empty/oversized; return 404 on missing id | CONDITIONAL |
| F-027 | MAJOR | Accessibility: unlabeled inputs (WCAG A) | Screen-reader users can't identify 30+ form inputs. | `AiAskTab.tsx:100-106` placeholder, no `<label>`; same across Todo/Pdr/Copybook/PromptInventory/Clipboard/Fleet; only 2 labels in 22 components | Inspect any tab input with screen reader | Placeholder used instead of label | Add `<label htmlFor>`/aria associations | CONDITIONAL |
| F-028 | MAJOR | Low contrast text (WCAG AA fail) | slate-500/600 on slate-950 ≈ 2.8:1 (<4.5:1) — unreadable for low vision. | `App.tsx:658,780,862,975` and components | Contrast-check any muted text | Design system ignored AA | Use slate-300/400 on dark; test all | CONDITIONAL |
| F-029 | MAJOR | clip-counts raw secret leakage (duplicate of F-003 store) | Secrets persist in a second file `clip-counts.json`. | `clip-history.ts:113-114` stores raw `value` for frequency; live grep `sk-ant` in `clip-counts.json` | Copy key; `cat clip-counts.json` | Frequency keyed by raw value | Hash value for counting; never store raw secret | REJECT |
| F-030 | MAJOR | Timezone/clock trust on timestamps | Wrong system clock → mis-ordered records; cloud trusts client time. | All stores use `new Date().toISOString()` (`persist.ts:93`, `todo-store.ts:66`) | Skew clock; create items; observe order | Client-side timestamping, no NTP | Server re-timestamps synced records | CONDITIONAL |
| F-031 | MAJOR | Multi-machine handoff incomplete / sync disabled | No "resume on device B"; `MachineSelector` stub; no pull-from-cloud; `CENTRAL_URL` unset. | `sync-client.ts` push-only; `MachineSelector.tsx` stub; `sync-machines.json` stale 06-23 | Open on PC-B → isolated state | Feature sketched not finished | Wire machine list + pull; document model | CONDITIONAL |
| F-032 | MAJOR | machineId/desktop key collisions on upsert | Two machines same hostname clobber each other on cloud. | `sync-store.ts:52-79` upsert by `machineId`; `sessions.ts:76-81` upsert by desktop, no lock | Two machines same name push concurrently | No unique id / conflict resolution | Strong machine UUID; composite keys; lastSeen | CONDITIONAL |
| F-033 | MAJOR | Capped collections trimmed after write | Files momentarily exceed cap; ad-hoc/inconsistent trimming. | `fleet-progress.ts:50-54`, `ask-history.ts:188-199`, `persist.ts:130-132` cap after append | Rapid telemetry; watch file size spike | Cap applied post-append | Cap before write (rolling buffer) | CONDITIONAL |
| F-034 | MAJOR | Screenshot/notebook orphaning (no atomicity) | Crash between file write and index → orphan PNG/file accumulates; or index says gone but file remains. | `screenshots-store.ts:60-87` two-phase; `notebook.ts:86-103` delete-then-unlink | Crash at `screenshots-store.ts:80`; count PNGs vs index | Two-phase write, no transaction | Index-first then capture; startup orphan sweep | CONDITIONAL |
| F-035 | MAJOR | CORS reflects any origin | Any visited website can read local API while auth off. | `server.ts:40-50` reflects Origin (line 42); comment assumes "no cookies" but session is a cookie | Malicious page `fetch('http://localhost:3000/api/clipboard/history')` | Origin reflection for APK/local convenience | Whitelist origins; require auth | CONDITIONAL |
| F-036 | MAJOR | Keystroke recorder dup-process / no persistent PID | Crash loses PID; restart spawns 2nd recorder; orphans accumulate. | `keylog-store.ts:97-115` PID file non-atomic; restart spawns new | Start; kill server; delete pid; restart; start → 2 processes | Detached process not tracked | Verify pid alive on boot; process manager | CONDITIONAL |
| F-037 | MAJOR | No encryption at rest for sensitive files | All clipboard/keystroke/screenshots/sessions plaintext; readable by any process/tenant. | `.gitignore:12-36` lists plaintext data files; `sync-store.ts:40-46` unencrypted | `cat clip-history.json` plaintext | Simplicity over security | Encrypt at rest (DPAPI/SQLCipher); chmod 600 | CONDITIONAL |
| F-038 | MAJOR | Shared production VPS blast radius (C8) | Veridian on shared commerce box; compromise of either risks the other; sensitive data co-located. | intent line 13 "shared with other people"; backup host `89.167.49.209` | Review deployment architecture | Cost-driven infra reuse | Dedicated VPS/container; isolation; retention policy | CONDITIONAL |
| F-039 | MAJOR | UX: loading has no timeout/cancel; dup-submission; inconsistent empty states; mobile layout | Hung requests show "Analyzing…" forever; confusing empty-state copy; APK preview breaks mobile. | `App.tsx:892-902` no timeout; `AiAskTab.tsx:104-114`; varied empty-state copy across tabs; `App.tsx:680` grid stacks | Disconnect during AI call → spinner forever | No AbortController/timeout; no shared patterns | AbortController+timeout+cancel; shared EmptyState; responsive APK panel | CONDITIONAL |
| F-040 | MAJOR | Clipboard restore not implemented cross-platform / dashboard widget read-only | Restore missing on Linux; autocomplete buried in tab not dashboard. | `clip-history.ts` restore via Set-Clipboard Windows only; dashboard `ClipboardHistory` read-only | Click history entry on Linux → nothing | Win-only restore; design split | Platform-detect restore; clarify dashboard vs tab | CONDITIONAL |
| F-041 | MINOR | Hardcoded "APK Ready v3.0" vs package.json 0.0.0 | Version confusion. | `App.tsx:655` hardcoded; `package.json` version 0.0.0 | Bump version; UI unchanged | Hardcoded string | Inject build version | OK |
| F-042 | MINOR | Stale "DeepSeek output / placeholder" comments | Code smell; misleading comments. | `App.tsx:328` comment references placeholder/DeepSeek | Read code | Comment not updated after fix | Update comments | OK |
| F-043 | MINOR | Voice fallback to Web Speech when no ElevenLabs key (C5) | "Natural voice" only with BYOK; robotic fallback otherwise. | `App.tsx:350-368` fallback; `.env` ELEVENLABS empty | Narrate with no key → robotic | BYOK model; intent language loose | Clarify intent doc; optional server-side TTS | OK |
| F-044 | MINOR | "fan-out" PDR term unmapped | Ambiguous requirement; fleet provides per-desktop parallelism. | No `/api/batch`; `AutopilotFleet.tsx` | grep fan-out → minimal | Ambiguous PDR term | Clarify PDR | OK |
| F-045 | MINOR | No ownership field on records | Future multi-user would have no isolation (PDR says single-user, so low risk now). | No owner/userId on any store; `desktop-briefs.ts:37` global | n/a | Single-user assumption | Out of scope unless multi-user | OK |
